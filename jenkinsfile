def groovyScript

pipeline {
    agent any 
    parameters{
        string(name: 'STAGE', defaultValue: 'BUILD', description: 'Stage to be executed')
    }
    stages {
        stage('Pipleline Initialization') { 
            steps {
                echo "Initializing the project."
                script{
                    groovyScript = load "script.groovy"
                }
            }
        }
        stage('Building Docker Image') { 
            steps {
                params.STAGE = "BUILD"
                script {
                    groovyScript.startingMessage()
                }
                mkdir '/Users/niran/Documents/Jenkins/Project'
                pwd
                script {
                    groovyScript.endingMessage()
                }
            }
        }
        stage('Running Docker Image') { 
            steps {
                params.STAGE = "RUNNING"
                script {
                    groovyScript.startingMessage()
                }


                script {
                    groovyScript.endingMessage()
                }
            }
        }
    }
    post {
        always{
            echo "Pipleline finished..."
        }
        success{
            echo "Pipleline finished successfully..."
        }
        failure{
            echo "Pipleline failure..."
        }
    }
}